
public class Sadness extends Emotion{

	Sadness(int negative){
		super();
		type = negative;
		//Calculates sadness 
		// amount = lastSocial/maxSocial;
		// if(p.age > p.repAge)
		// double socialAmount = lastRep/maxRep;
		// amount = (amount + socialAmount) / 2;
	}
}
